# Unpinned - the previous pinned versions (numpy 1.17, pandas 0.25, PyTorch
# not even listed, ...) predate this file's model additions and won't build
# on a modern Python/Kaggle/Colab environment. `torch` is listed for
# completeness on a fresh machine; on Kaggle/Colab it's already installed
# and `pip install torch` with no version pin is a no-op against an
# existing install, so it won't downgrade their CUDA-matched build.
torch
torch_geometric
torchdiffeq       # AirDDE, AirPhyNet, AirDualODE
pytorch_wavelets  # WPMixer's wavelet decomposition (DWT1DForward/DWT1DInverse)
PyWavelets        # pytorch_wavelets' actual runtime dependency (imports `pywt`
                  # internally to fetch filter coefficients by wavelet name) -
                  # not declared in pytorch_wavelets' own package metadata, so
                  # listed explicitly here. Pure prebuilt wheels on every major
                  # platform (unlike torch_scatter's CUDA-build coupling this
                  # repo already avoids elsewhere) - safe, lightweight add.
numpy
scipy
PyYAML
arrow
tqdm
geopy
MetPy
bresenham
psutil            # peak-memory reporting on CPU runs (GPU runs use torch.cuda's own stats)

# notebooks/EDA_temporal_spatial.ipynb
pandas
matplotlib
seaborn
statsmodels       # ACF/PACF plots
jupyter
